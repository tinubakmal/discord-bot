# SQLite en local (par defaut). Pour une future mise en ligne, remplacer par une URL
# Postgres (ex: postgresql://user:password@host:5432/wizard101db) et changer le
# provider dans prisma/schema.prisma de "sqlite" a "postgresql".
DATABASE_URL="file:./dev.db"
